# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoogleSDk_ demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GoogleSDk_ demo
  pod 'GooglePlaces'
  pod 'GooglePlacePicker'
  pod 'GoogleMaps'

  target 'GoogleSDk_ demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoogleSDk_ demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
